import 'package:app/lib.dart';

class Slot extends StatelessComponent {
  const Slot({super.key});

  @override
  Iterable<Component> build(BuildContext context) => [];

  @css
  static final List<StyleRule> styles = [];
}
