import 'package:app/lib.dart';

class HomeBody extends AppStatelessComponent {
  const HomeBody({
    super.key,
    super.size,
    super.state,
    super.themeMode,
  });

  @override
  Iterable<Component> build(BuildContext context) sync* {
    yield section([]);
  }
}
